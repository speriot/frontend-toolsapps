apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "frontend-toolsapps.fullname" . }}-test-connection"
  labels:
    {{- include "frontend-toolsapps.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "frontend-toolsapps.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

