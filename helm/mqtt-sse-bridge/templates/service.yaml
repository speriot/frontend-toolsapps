apiVersion: v1
kind: Service
metadata:
  name: {{ include "mqtt-sse-bridge.fullname" . }}
  labels:
    {{- include "mqtt-sse-bridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "mqtt-sse-bridge.selectorLabels" . | nindent 4 }}
